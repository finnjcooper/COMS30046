# RISC-V SIMULATOR

Prerequisites
---

make, cmake, ninja  
gcc 15.2+  
riscv64-unknown-elf-gcc

# Instructions

The program is built using ninja build files that are generated by cmake.
A makefile is included in the root directory to automate this process.

`make cmake` will generate the ninja build files. This has to be done only once.

`make` will then build and run the simulator.

Flags
---

`BENCH` will set the benchmark c file to run from the /test folder (add.c by default), e.g. `make BENCH=vectoradd` - omit the .c  
New c programs can be written and simulated by placing them in /test and specifying with `BENCH=...`.

`PIPELINED` will set whether to use pipelined execution (true by default), e.g. `make PIPELINED=false`

`FORWARDING` will set whether to use operand forwarding (true by default), e.g. `make BENCH=branch FORWARDING=false`

Terminal Interface
---

Once running, basic controls are shown in the bottom right corner:

`h` - show a popup with all controls.

`SPACE` / `ENTER` - advance the simulation once cycle.

`r` - run the simulation until the end of the program.

`TAB` - cycle between the register view and the stack view in the right-hand panel.  
Note that for now the stack only shows what is between the current stack pointer and the end of memory.

`q` - quit the simulator.
